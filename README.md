# Cease and Desist Sans

Take a set of TTF fonts and generate a combined WOFF2 font.


## Build

Build the webserver container.

```bash
docker-compose build .
```

## Run

Run the webserver container.

```bash
docker compose -f compose.yml up -d
```
